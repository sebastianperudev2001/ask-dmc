# AI-DLC State Tracking

## Project Information
- **Project Name**: DMC Sales Agent
- **Project Type**: Greenfield
- **Start Date**: 2026-04-23T00:00:00Z
- **Current Stage**: INCEPTION - BackOffice lead qualification view - Requirements Analysis COMPLETED, awaiting user approval. Previous initiative (CONSTRUCTION - agent-service incremento 2 + apps/chat) Build and Test COMPLETED (3 rondas de verificación E2E real: 2 rondas de bugs corregidos + 1 ronda de feature nueva — rehidratación de mensajes al recargar). Pendiente: aprobación final del usuario; pago Mercado Pago sin probar (sin credenciales)
- **Platform**: Azure (pivote desde AWS el 2026-07-05 — ver DIV-10). unit-1 (ingestion-pipeline) fue eliminada el 2026-07-07 tras confirmarse que nunca quedó conectada a `agent-service` — ver DIV-15.

## Workspace State
- **Existing Code**: No
- **Reverse Engineering Needed**: No
- **Workspace Root**: /Users/sebastianchavarry01/Documents/personal-github/ask-dmc

## Code Location Rules
- **Application Code**: Workspace root (NEVER in aidlc-docs/)
- **Documentation**: aidlc-docs/ only
- **Structure patterns**: See code-generation.md Critical Rules

## Extension Configuration

| Extension | Enabled | Decided At |
|---|---|---|
| Security Baseline (SECURITY-01 a SECURITY-15) | Yes | Requirements Analysis |
| Property-Based Testing (PBT-01 a PBT-10) | Yes — Full enforcement | Requirements Analysis |

## Known Divergences from Inception Decisions

| # | Decision in Inception | Divergence in Construction | Rationale | Affected Inception Docs |
|---|---|---|---|---|
| DIV-01 | `PDFExtractor` uses `LLMProvider` to extract 12 sections from brochures (Requirements Analysis, Application Design) | Functional Design (unit-1) replaces LLM extraction with a deterministic pdfplumber + regex parser | Brochure content structure is predictable and consistent; deterministic parsing is more reliable and eliminates LLM cost for this step | `requirements.md`, `components.md`, `services.md`, `unit-of-work.md`, `stories.md` |
| DIV-02 | `KeywordsExtractor.extract_keywords(sections) → list[BrochureSection]` — extracción por sección, keywords en `BrochureSection.keywords`, 3 retries con backoff (business-logic-model.md) | `extract_keywords(sections) → list[str]` — extracción a nivel de documento combinando 5 secciones clave, retorna una sola lista de keywords por brochure, sin retry, salida estructurada con Pydantic | Keywords a nivel de documento son más representativas para búsqueda; reduce de N llamadas LLM a 1 por PDF; Pydantic structured output elimina parsing manual de JSON | `business-logic-model.md`, `domain-entities.md` |
| DIV-03 | `EmbeddedChunk` tiene `section_type: SectionType`; `BrochureSection` tiene `keywords: list[str]` (domain-entities.md) | Ambos campos eliminados: `EmbeddedChunk` sin `section_type`, `BrochureSection` sin `keywords` | Consecuencia de DIV-02: keywords son documento-nivel; section_type no aporta valor en el vector DB para recuperación semántica | `domain-entities.md`, `migrations/001` |
| DIV-04 | `EmbeddingGenerator.generate(sections) → list[EmbeddedChunk]` — un chunk por sección, texto enriquecido `Programa:/Sección:`, keywords de `section.keywords` (business-logic-model.md) | `generate(sections, keywords: list[str]) → list[EmbeddedChunk]` — text-splitting 2000 chars / 200 overlap, formato `[section_type]\ncontent`, keywords externos aplicados a todos los chunks | Secciones largas excedían límite de tokens del modelo de embeddings; keywords externos vienen de DIV-02 | `business-logic-model.md` |
| DIV-05 | `brochure_chunks` table: `section_type TEXT NOT NULL`, `embedding vector(1536)` (migrations/001) | Columna `section_type` eliminada; `embedding vector(768)`; nueva migración 002 trunca la tabla | section_type eliminado por DIV-03; nomic-embed-text produce 768 dims (no 1536, que era para Bedrock Titan) | `migrations/001_create_brochure_chunks.sql` |
| DIV-06 | PATTERN-03: Retry con backoff en `OllamaEmbeddingsProvider.embed()` (nfr-design-patterns.md, post-build fix 2026-04-30) | Retry eliminado; reemplazado por `keep_alive=10m` en el payload de la llamada a Ollama | Causa raíz eran descargas de modelo entre llamadas (model swap); `keep_alive` previene la descarga, eliminando la condición — retry trataba el síntoma, no la causa | `nfr-design-patterns.md` (PATTERN-03) |
| DIV-07 | `LLMProvider.complete(prompt: str) → str` (ports/llm_provider.py) | `complete(prompt: str, format: dict | None = None) → str`; OllamaLLMProvider pasa `format` al payload para structured output | Consecuencia de DIV-02: keywords usan JSON schema de Pydantic como `format` para forzar salida estructurada válida | `ports/llm_provider.py`, `infrastructure/llm/ollama_llm.py`, `infrastructure/llm/bedrock_llm.py` |
| DIV-08 | `ingestion_workers=4 if env == ENV.LOCAL else int(os.environ.get("INGESTION_WORKERS", "4"))` (src/config.py) | `ingestion_workers=int(os.environ.get("INGESTION_WORKERS", "4"))` — siempre desde el entorno sin condicional por ENV | `.env` debe ser la única fuente de verdad; el hardcode para LOCAL ignoraba silenciosamente el valor en `.env` | `src/config.py` |
| DIV-09 | `EmbeddingGenerator` y `test_rag.py` usaban texto crudo al llamar a `OllamaEmbeddingsProvider.embed()` (sin prefijo) | `embed(f"search_document: {text}")` al indexar chunks; `embed(f"search_query: {question}")` al buscar | `nomic-embed-text` es un modelo asimétrico: requiere prefijos distintos para documentos y consultas para producir similitudes coseno bien alineadas. Sin ellos, la recuperación degrada significativamente. Acción posterior requerida: re-ejecutar la ingesta completa para recomputar embeddings con el prefijo correcto. | `src/pipeline/embedding_generator.py`, `scripts/test_rag.py` |
| DIV-10 | `unit-2: strands-agent` — AWS Strands SDK + AgentCore Memory/Runtime + Bedrock (application-design.md, unit-of-work.md, strands-agent Functional Design completado 2026-07-01) | Pivote de plataforma: `unit-2` se redefine como `agent-service` sobre **Azure AI Foundry + Microsoft Agent Framework**. El Functional Design de `strands-agent` (AWS) queda **SUPERSEDED** — no se descarta el archivo (histórico) pero no se implementará. Primer incremento del nuevo `agent-service`: catálogo de cursos + recomendación por perfil (filtros duros: presupuesto/duración + ranking semántico sobre background profesional/stack deseado), NO el flujo completo de identificación/calificación (queda como incremento siguiente dentro de la misma unidad redefinida) | Decisión explícita del usuario (2026-07-05): "necesito centrarme en crear el backend del producto en Azure usando foundry y agent framework de azure". Reemplazo total confirmado vía pregunta de alcance. `unit-1` (ingestion-pipeline, AWS/Bedrock) **no** se re-plataforma en este momento — no fue parte de esta decisión y sigue funcionando como está. | `application-design.md`, `unit-of-work.md`, `construction/strands-agent/functional-design/*` (todos superseded) |
| DIV-11 | ~~RF-08: Mercado Pago Checkout API (sandbox)~~ → Culqi (propuesto en Requirements Analysis 2026-07-06) | **REVERTIDO** (2026-07-06, durante Functional Design): el usuario descartó Culqi ("necesito un negocio oficial") y pivoteó de vuelta a **Mercado Pago**, ahora vía **Checkout Pro** (API de Preferencias → `init_point` como URL de checkout ya hospedada por Mercado Pago, sin página de checkout propia) + webhook con verificación de firma HMAC real (`x-signature`/`x-request-id`, documentada oficialmente — a diferencia de Culqi) | Culqi requeriría una cuenta de negocio formal que el usuario no tiene; Mercado Pago es accesible sin ese requisito y además simplifica la arquitectura (no requiere página de checkout propia ni el workaround de re-consultar la API como mitigación de falta de firma) | `requirements.md` §5.1 RF-08, `frontend-integration-requirements.md` RF-I10 a RF-I13 (actualizados) |
| DIV-12 | RF-09: notificación de escalación a humano vía Lambda + Amazon SES | Sin notificación activa en el incremento 2 — solo se persiste `escalated_to_human=true`; el envío proactivo (canal aún no decidido: Azure Communication Services / Slack / Teams) queda diferido a un incremento futuro | No hay equivalente de SES decidido en Azure aún; el usuario eligió diferir explícitamente en vez de asumir un reemplazo (2026-07-06) | `requirements.md` §5.1 RF-09, `frontend-integration-requirements.md` RF-I08 |
| DIV-13 | §6: persistencia de leads y conversaciones en DynamoDB (`dmc-leads`, `dmc-conversations`) | Persistencia en **Postgres** (mismo motor ya usado por `agent-service` para el catálogo de cursos) | Consistente con el pivote de plataforma a Azure (DIV-10); evita introducir DynamoDB en una arquitectura ya centrada en Postgres/Azure | `requirements.md` §6, `frontend-integration-requirements.md` RF-I14, RF-I15 |
| DIV-14 | RF-11 (visión original): streaming WebSocket de un solo turno; incremento 1 de `agent-service`: `recommendation_request` estructurado de un solo turno (`schemas.py`) | Protocolo conversacional multi-turno con tool-calling (`collect_profile_data`, `create_payment_link`) sobre el mismo endpoint, renombrado de `/ws/recommendation` a `/ws/chat` | Decisión explícita del usuario (2026-07-06): mantener chat libre como experiencia principal ("al final lo que estamos haciendo es un agent de ventas"); tools se invocan solo cuando el agente determina que necesita datos estructurados o generar un pago | `requirements.md` §5.1 RF-11, `src/api/schemas.py`, `frontend-integration-requirements.md` RF-I01 a RF-I04 |
| DIV-15 | `unit-1: ingestion-pipeline` (PDF brochures → embeddings → pgvector `brochure_chunks`, ver `application-design.md`/`unit-of-work.md`) alimentando el RAG del agente | `services/ingestion/` **eliminado por completo** (2026-07-07). Nunca quedó conectado al `agent-service` real: corre contra un Postgres distinto (puerto 5433, DB `ask_dmc`) del que usa `agent-service` (puerto 5434, DB `agent_service`), y el catálogo real del agente sale de `scripts/catalog_seed_data.json` (curado a mano), no de `brochure_chunks`. `knowledge_source/` (los PDFs) se conserva por si se retoma el enfoque RAG, pero hoy no lo consume nada | Encontrado durante una limpieza de código muerto pedida por el usuario ("hay mucho código basura y muerto que no me permite entender el flujo actual"); confirmado que el pipeline nunca fue re-cableado tras el pivote DIV-10/DIV-14 a `agent-service`. Usuario confirmó el borrado explícitamente | `application-design.md`, `unit-of-work.md` (unit-1 queda como diseño histórico sin implementación viva), `README.md` |

> Inception docs are kept as historical record. Construction artifacts take precedence for implementation.

---

## Stage Progress

### INCEPTION PHASE
- [x] Workspace Detection — COMPLETED (2026-04-23)
- [x] Requirements Analysis — COMPLETED and APPROVED (2026-04-28)
- [x] User Stories — COMPLETED and APPROVED (2026-04-28)
- [x] Workflow Planning — COMPLETED and APPROVED (2026-04-28)
- [x] Application Design — COMPLETED and APPROVED (2026-04-28)
- [x] Units Generation — COMPLETED and APPROVED (2026-04-28)

### CONSTRUCTION PHASE
- [x] Per-Unit Loop — unit-1: ingestion-pipeline COMPLETED (2026-04-30)
  - [x] Functional Design — COMPLETED and APPROVED
  - [x] NFR Requirements — COMPLETED and APPROVED
  - [x] NFR Design — COMPLETED and APPROVED
  - [x] Infrastructure Design — COMPLETED and APPROVED
  - [x] Code Generation — COMPLETED (awaiting approval)
- [x] Build and Test (unit-1) — COMPLETED (2026-04-30)
- [x] Per-Unit Loop — unit-2: strands-agent (AWS) — SUPERSEDED (2026-07-05, ver DIV-10)
  - [x] Functional Design — COMPLETED but SUPERSEDED (never approved; discarded por pivote de plataforma)
- [ ] Per-Unit Loop — unit-2 (redefinida): agent-service (Azure) — IN PROGRESS (started 2026-07-05)
  - [x] Functional Design — COMPLETED and APPROVED (2026-07-05, incremento 1: catálogo de cursos + recomendación por perfil, 3 rondas de enmienda)
  - [x] NFR Requirements — COMPLETED and APPROVED (2026-07-05, Container Apps + Foundry Persistent Agent, Postgres+pgvector, gpt-4o-mini, East US; SECURITY-11 abierto/riesgo aceptado)
  - [x] NFR Design — COMPLETED and APPROVED (2026-07-05, 13 patterns + 9 logical components; timeout relax=5min, min 1 réplica, retry 3x backoff)
  - [x] Infrastructure Design — COMPLETED and APPROVED (2026-07-05, Terraform, Container Apps + Postgres Burstable B1ms + Azure OpenAI + Foundry Persistent Agent, resource group único rg-dmc-agent-service)
  - [x] Code Generation — COMPLETED and APPROVED (2026-07-05, 24/24 pasos; suite verificada 20 passed/2 skipped tras fix de fixtures) — `services/agent-service/` + `infra/agent-service/`
- [x] Build and Test (agent-service, incremento 1) — COMPLETED (2026-07-06) — 22/22 tests passed; flujo WS completo verificado end-to-end contra Postgres (Docker) + Azure OpenAI real + Foundry Persistent Agent real (`gpt-5.4-nano-dmc-bicep`); primer delta medido en 3.95s (objetivo ≤3s, levemente excedido). Pendiente explícito: despliegue real a Azure Container Apps (`infra/agent-service/*.tf` sin aplicar — fuera de alcance decidido por el usuario para este incremento)

### 🔵 INCEPTION — agent-service incremento 2 (chat conversacional + pago + integración apps/chat)
- [x] Workspace Detection — reutilizado (proyecto existente, brownfield ya establecido)
- [x] Requirements Analysis — COMPLETED and APPROVED (2026-07-06, 3 rondas de clarificación) — `aidlc-docs/inception/requirements/frontend-integration-requirements.md`
- [x] User Stories — SKIPPED (comportamiento de usuario ya cubierto por `stories.md` de la visión original; este incremento es reimplementación técnica, no nueva funcionalidad de cara al usuario)
- [x] Workflow Planning — COMPLETED (2026-07-06) — `aidlc-docs/inception/plans/frontend-integration-execution-plan.md` — Application Design y Units Generation SKIP (sin nuevas unidades); `apps/chat` formalizado como unidad activa (equivalente a la antigua `unit-4: frontend-widget`)
- [ ] Application Design — SKIPPED (ver rationale en execution plan)
- [ ] Units Generation — SKIPPED (ver rationale en execution plan)

### 🟢 CONSTRUCTION — agent-service incremento 2 + apps/chat
- [x] Per-Unit Loop — unit-2: agent-service (incremento 2) — COMPLETED (2026-07-06)
  - [x] Functional Design — COMPLETED and APPROVED — spike técnico real ejecutado (tool-calling detectable en streaming, auto-ejecución confirmada, sesión vía Foundry Memory confirmada); pivote de proveedor de pago Culqi→Mercado Pago durante esta etapa (DIV-11 revertido) — `aidlc-docs/construction/agent-service/functional-design/{business-logic-model,business-rules,domain-entities}.md` (secciones "Incremento 2")
  - [x] NFR Requirements — COMPLETED and APPROVED — conflicto detectado y resuelto (webhook público vs. alcance local: se testea con simulación manual, sin exposición pública en este incremento); idempotencia del webhook confirmada — `aidlc-docs/construction/agent-service/nfr-requirements/{nfr-requirements,tech-stack-decisions}.md` (secciones "Incremento 2")
  - [x] NFR Design — COMPLETED and APPROVED — PATTERN-14 a PATTERN-20 + 9 componentes lógicos nuevos — `aidlc-docs/construction/agent-service/nfr-design/{nfr-design-patterns,logical-components}.md` (secciones "Incremento 2")
  - [x] Infrastructure Design — COMPLETED and APPROVED — sin recursos nuevos (mismo Container App/Postgres/Key Vault); nueva migración + 2 secrets + ruta webhook sin exposición pública — `aidlc-docs/construction/agent-service/infrastructure-design/{infrastructure-design,deployment-architecture}.md` (secciones "Incremento 2")
  - [x] Code Generation — COMPLETED and APPROVED (2026-07-06) — Parte 1 (plan de 39 pasos) + Parte 2 (implementación completa: dominio, tools, WS handler, webhook, repos, migración, scripts, tests — 42 passed/6 skipped backend). 3 gaps de diseño reales encontrados y corregidos durante la generación (tool de recomendación faltante, correlación webhook↔lead, persistencia de Lead nunca ocurría) + evento `turn_done` agregado — `aidlc-docs/construction/plans/agent-service-increment2-code-generation-plan.md`
- [x] Per-Unit Loop — unit apps/chat — COMPLETED (2026-07-06)
  - [x] Functional Design (ligero) — COMPLETED and APPROVED — `aidlc-docs/construction/apps-chat/functional-design/frontend-components.md`
  - [x] Code Generation — COMPLETED and APPROVED (2026-07-06) — WsChatService, useChat reescrito, ProfileDataWidget, CourseRecommendationCard — 19/19 tests, tsc limpio, `next build` exitoso — `aidlc-docs/construction/apps-chat/code/frontend-components-summary.md`
- [x] Build and Test (agent-service incremento 2 + apps/chat) — COMPLETED (2026-07-06) — verificación E2E real con navegador (Playwright, instalado ad-hoc), Postgres+pgvector real (Docker), catálogo real, agente Foundry real. Flujo completo confirmado: chat libre → `collect_profile_data` (widget con prefill real) → submit → `get_course_recommendations` (sin match exacto → oferta conversacional de ampliar rango → aceptada → candidato real encontrado) → `CourseRecommendationCard` renderizado → `Lead` persistido correctamente → mensajes rehidratados correctamente tras recargar la página. **5 bugs reales encontrados y corregidos** (Rondas 1-2) + **1 feature nueva agregada y verificada** (Ronda 3, a pedido del usuario):
  - Ronda 1 — (1) desconexión de WS a mitad de turno no manejada, (2) `service_session_id` de Foundry rota por turno (fragmentaba el `Lead`, corregido con `conversation_id` estable).
  - Ronda 2 (reportados por el usuario, investigados con `superpowers:systematic-debugging`) — (3) `collect_profile_data` sin timeout deadlockeaba la conexión completa si el usuario abandonaba el widget sin desconectarse (revierte parcialmente Clarification NFR Q1=C), (4) "Nueva conversación" no cerraba la conexión ni el `service_session_id` guardado, (5) bug propio: el primer intento de arreglar (4) mantuvo un guard `if (busy) return` que bloqueaba justo el escenario que debía arreglar.
  - Ronda 3 (feature solicitada tras explicar dónde se guardan las conversaciones) — persistencia propia del transcript (`conversation_messages`, migración 003) y endpoint `GET /conversations/{id}/messages` para rehidratar mensajes visibles al recargar (Foundry no expone API de lectura de historial vía el SDK usado); `conversation_id` ahora también se retoma en reconexión, cerrando la limitación de `Lead` duplicado en reload. Bug propio encontrado y corregido: una edición descuidada de `tests/integration/fakes.py` partió la clase `FakeLeadRepository`. Bug preexistente latente corregido: `test_postgres_lead_repository.py` fallaba contra Postgres real por un TRUNCATE sin CASCADE (nunca antes ejecutado contra DB real en esta sesión).
  - Ronda 4 (feature solicitada tras confirmar que el historial del Sidebar seguía siendo mock: "el historial de conversaciones a la izquierda en el front no jala las sesiones creadas?", aprobada con "si dale") — historial real de conversaciones: `ConversationMessageRepository.list_conversations()` + endpoint `GET /conversations`; nuevo `ConversationSessionStore` (repositorio Postgres sobre la tabla `conversation_sessions`, sin uso hasta ahora) para poder resumir el hilo de Foundry correcto de CUALQUIER conversación pasada, no solo la más reciente que recuerda el `localStorage`; `Sidebar.tsx` ya no usa `data/mock.ts`. Verificado con Playwright real: crear 2 conversaciones, recargar, confirmar que el Sidebar muestra ambas con preview real, clic en la más antigua retoma su `conversation_id` y su mensaje correctamente. Suite tras esta ronda: 58/58 backend, 24/24 frontend, `tsc --noEmit` limpio.
  - Ronda 5 (bug reportado por el usuario: "No se estan obteniendo los mensajes dentro de una conversacion", investigado con `superpowers:systematic-debugging`) — causa raíz: `asyncio.CancelledError` (BaseException desde Python 3.8, no capturado ni por `except Exception` ni por `except (WebSocketDisconnect, RuntimeError)`) escapaba de `handle_connection` cuando se abandonaba el widget de `collect_profile_data` y la conexión se cerraba, saltándose la persistencia de la respuesta del bot. Reproducido con un `FakeWebSocket` de bajo nivel (TestClient no dejaba escapar el crash); confirmado con el usuario que las conversaciones "rotas" eran las de la Ronda 4. Fix: `except asyncio.CancelledError` explícito antes del `except Exception` existente. Suite tras el fix: 60/60 backend. Hallazgo incidental no relacionado y no corregido: `test_p6_pgvector_ranking_matches_numpy_oracle` (preexistente del incremento 1, edge case de floats extremos).
  - Ronda 6 (bug reportado por el usuario: `ParseError` en el frontend tras enviar el widget — `NaN` en `similarity_score`) — causa raíz doble: (1) `TEST_DATABASE_URL` compartía la MISMA base Postgres que el demo durante toda la sesión, y el `test_p6_pgvector_ranking_matches_numpy_oracle` (hallazgo de la Ronda 5) truncaba `courses` en cada corrida de la suite, dejando el catálogo real reducido a un curso sintético con embedding cero; (2) un embedding de magnitud cero hace indefinida la distancia coseno de pgvector → `similarity_score = NaN`, y `json.dumps` emite el literal `NaN` (inválido para JSON estándar), rechazado por `JSON.parse` del navegador. Fix de datos: base de test aislada (`agent_service_test`, mismo contenedor Docker, migraciones aplicadas) para que los tests dejen de poder dañar el catálogo del demo; catálogo real re-sembrado (`scripts.seed_catalog`). Fix de código (TDD): `CandidateSummary.from_candidate` sanea `NaN` a `0.0` antes de serializar (nuevo test `tests/unit/test_schemas.py`). Verificado con Playwright real: envío del widget sin errores de consola. Suite tras el fix: 61/62 backend (único fallo: el mismo Hypothesis preexistente, ya inofensivo para el catálogo real).
  - Pago (Mercado Pago) explícitamente no probado — sin credenciales sandbox (decisión del usuario). Sin despliegue a Azure ni `terraform plan/apply` — alcance exclusivamente local. Detalle completo en `aidlc-docs/construction/build-and-test/build-and-test-summary.md` y `business-logic-model.md` Secciones 14-18.
  - Ronda 7 (feature solicitada tras explicar que `score_lead()` (BR-17) existía pero nunca se invocaba desde el flujo real — gap encontrado durante una pregunta exploratoria del usuario, no un bug reportado) — nuevo BR-17b: piso de engagement monotónico sobre `Lead.score` basado en conteo de mensajes del usuario (5+ → `warm`, 10+ → `hot`) y en completar el formulario de perfil (`warm`), combinado con `score_lead()` tomando siempre el máximo, sin bajarlo nunca. Implementado con TDD (RED-GREEN verificado en cada pieza): `engagement_floor`/`apply_score_floor` puras en `src/domain/lead_scoring.py`; `ChatAgentClient.record_user_message()`/`_apply_engagement_floor()` (invocado tras cada mensaje de usuario y tras `_collect_profile_data`); `ChatWebSocketHandler` llama a `record_user_message()` una vez por turno. Suite verificada sin regresiones: 64/64 backend (12 skipped), +13 tests nuevos. Pendiente sin cambios: extracción real de `motivation`/`purchase_intent`/`has_complete_data` desde la conversación libre (ya documentado como gap futuro en `business-logic-summary-increment2.md`) — BR-17b es un complemento pragmático, no la reemplaza.

### 🔵 INCEPTION — BackOffice lead qualification view
- [x] Workspace Detection — reutilizado (proyecto brownfield ya establecido vía AI-DLC)
- [x] Requirements Analysis — COMPLETED and APPROVED (2026-07-09, 2 rondas iniciales + 1 amendment durante User Stories) — `aidlc-docs/inception/requirements/backoffice-requirements.md`
  - Scope: nueva app `apps/backoffice` (Next.js, standalone), kanban read-only (Hot/Warm/Cold) sobre `Lead`, tiempo real vía nuevo canal WebSocket, nuevo `list_leads`/`GET /leads` en `agent-service`. Sin auth en esta iteración — seguimiento en [GitHub issue #18](https://github.com/sebastianperudev2001/ask-dmc/issues/18). Identidad visual separada de `apps/chat`, alineada a brand guidelines de dmc.pe, vía skill `frontend-design` en Code Generation.
  - **Ampliado durante User Stories** (FR-9 a FR-14, NFR-5/NFR-6): agente de outreach (auto-draft en `hot` o on-demand, gate humano obligatorio antes de enviar, solo email este incremento — WhatsApp diferido a [issue #19](https://github.com/sebastianperudev2001/ask-dmc/issues/19)) + notificación in-app reutilizando el canal WS. Proveedor de email diferido a NFR Requirements.
- [x] User Stories — COMPLETED and APPROVED (2026-07-09) — persona única "DMC Sales Staff", 7 historias feature-based con criterios Given/When/Then — `aidlc-docs/inception/user-stories/backoffice-{stories,personas}.md`
- [x] Workflow Planning — COMPLETED and APPROVED (2026-07-10) — `aidlc-docs/inception/plans/backoffice-execution-plan.md`. Plan: Application Design EXECUTE, Units Generation EXECUTE (2 unidades: `agent-service` extendida + nueva `apps/backoffice`), luego per-unit loop completo para `agent-service` (Functional/NFR Requirements/NFR Design/Infra Design/Code Gen) y ligero para `apps/backoffice` (Functional Design ligero + Code Gen, sin NFR/Infra — mismo patrón que `apps/chat` en incremento 2), luego Build and Test.
- [x] Application Design — COMPLETED and APPROVED (2026-07-10) — 7 preguntas respondidas (OutreachAgentService standalone, LeadEventPublisher pub/sub único, OutreachDraft como entidad nueva con DraftRepository propio, sin DraftLifecycleService separado, sin GET /leads/{id} dedicado, apps/backoffice modular, EmailSender como puerto definido ahora) — `aidlc-docs/inception/application-design/backoffice-{components,component-methods,services,component-dependency,application-design}.md`
- [x] Units Generation — COMPLETED and APPROVED (2026-07-10) — 3 preguntas respondidas (Incremento 3 numerado secuencialmente, build estrictamente secuencial agent-service→apps/backoffice sin mocks, apps/backoffice standalone sin Turborepo — spike de Turborepo trackeado en [GitHub issue #20](https://github.com/sebastianperudev2001/ask-dmc/issues/20)) — `aidlc-docs/inception/application-design/backoffice-unit-of-work{,-dependency,-story-map}.md`

### 🟢 CONSTRUCTION — agent-service Incremento 3 (BackOffice: read path + broadcast + outreach agent) + apps/backoffice
- [ ] Per-Unit Loop — unit agent-service (Incremento 3) — IN PROGRESS (started 2026-07-11)
  - [x] Functional Design — COMPLETED and APPROVED (2026-07-11, 9 preguntas + 2 follow-ups) — dedupe de drafts solo por status=pending, regen on-demand retorna el pending existente sin recrear, auto-trigger fire-and-forget (no bloquea el turno de chat), skip silencioso si falta email solo en el trigger automático, `OutreachAgentService` agentic con tool `get_course_details` (mismo patrón que `ChatAgentClient`), errores LLM propagados on-demand/absorbidos en el trigger automático, envío fallido deja el draft en `pending` sin status `failed` nuevo, `LeadEvent` lleva el `Lead` completo, snapshot de reconexión sin indicador de draft — secciones "Incremento 3" añadidas a `aidlc-docs/construction/agent-service/functional-design/{business-logic-model,business-rules,domain-entities}.md`
  - [x] NFR Requirements — COMPLETED and APPROVED (2026-07-11, 5 preguntas + 1 follow-up) — Azure Communication Services (Email) como proveedor, sin safelist de destinatarios (riesgo aceptado explícito), guard de envío idempotente en 2 capas (loading state en frontend + `UPDATE ... WHERE status='pending'` atómico en backend), `agent-service` fijo en min=max=1 réplica (restricción de correctitud, no solo costo — `LeadEventPublisher`/`LeadBroadcaster` son en memoria), sin SLA de latencia para generación de drafts — secciones "Incremento 3" añadidas a `aidlc-docs/construction/agent-service/nfr-requirements/{nfr-requirements,tech-stack-decisions}.md`
  - [x] NFR Design — COMPLETED and APPROVED (2026-07-11, 2 preguntas) — PATTERN-21 a PATTERN-28 (retry con backoff para email, excepción explícita sin retry para la llamada LLM del draft, generación en background/fire-and-forget, detección perezosa de conexiones muertas en /ws/leads, restricción dura de 1 réplica, validación de email antes de enviar, redacción de contenido de draft en logs, guard atómico de envío único) + 8 componentes lógicos nuevos (`LeadQueryService`, `LeadEventPublisher`, `LeadBroadcaster`, `LeadRepository` extendido, `OutreachAgentService` agentic, `GetCourseDetailsTool`, `DraftRepository`/`PostgresDraftRepository`, `EmailSender`/`AzureCommunicationServicesEmailSender`) — secciones "Incremento 3" añadidas a `aidlc-docs/construction/agent-service/nfr-design/{nfr-design-patterns,logical-components}.md`
  - [x] Infrastructure Design — COMPLETED and APPROVED (2026-07-11, 3 preguntas respondidas por el AI a pedido del usuario de avanzar más rápido) — recurso Azure nuevo: Azure Communication Services (Email, dominio administrado); nuevo secreto Key Vault `acs-connection-string`; `max_replicas` de `agent-service` bajado de 3 a 1 (PATTERN-25, restricción de correctitud); migración de `outreach_drafts` diferida a Code Generation — `infra/agent-service/main.tf` actualizado (escrito, no aplicado) + secciones "Incremento 3" en `aidlc-docs/construction/agent-service/infrastructure-design/{infrastructure-design,deployment-architecture}.md`
  - [x] Code Generation — COMPLETED and APPROVED (2026-07-11, 18 pasos, 1 commit por paso) — nuevos: `lead_event_publisher.py`, `lead_query_service.py`, `draft_repository.py`/`email_sender.py` (puertos), `postgres_draft_repository.py`, `acs_email_sender.py`, `outreach_agent_service.py` (agentic, tool `get_course_details`), `lead_broadcaster.py`, `migrations/004_create_outreach_drafts.sql` (+ índice único parcial para BR-22); extendidos: `models.py`, `chat_agent_client.py` (1 punto de publicación), `lead_repository.py`/`course_repository.py`, adaptadores Postgres correspondientes, `schemas.py`, `config.py`, `main.py` (5 rutas nuevas). Gap real encontrado y corregido: construcción eager de `EmailClient` rompía el arranque de `main.py` sin `ACS_CONNECTION_STRING` — corregido con construcción perezosa. Verificación con Postgres real (contenedor `agent-service-db` reiniciado, migración 004 aplicada): encontrado y corregido un bug real (FK nueva de `outreach_drafts` rompía `TRUNCATE TABLE conversation_sessions, leads` en tests existentes). Suite final: 116 passed, 1 failed (`test_p6_pgvector_ranking_matches_numpy_oracle`, flaky preexistente de incremento 1, ya documentado, no relacionado a este incremento) — `aidlc-docs/construction/agent-service/code/{business-logic,api-layer,repository-layer}-summary-increment3.md`
- [ ] Per-Unit Loop — unit apps/backoffice (new) — IN PROGRESS (empieza tras Code Generation de agent-service, build estrictamente secuencial)
  - [ ] Functional Design (light) — pendiente
  - [ ] Code Generation — pendiente
- [ ] Build and Test — pendiente

### OPERATIONS PHASE
- [ ] Operations — PLACEHOLDER
